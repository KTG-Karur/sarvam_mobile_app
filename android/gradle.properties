org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Android Gradle Plugin 8.11 requires JDK 17+. Pin Gradle to the installed
# JDK so local terminals do not accidentally select the system JDK 11.
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.18.8-hotspot
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Disabled: Kotlin incremental compilation crashes computing relative paths
# between the project (E:) and the pub cache (C:) on Windows.
kotlin.incremental=false
android.enableR8.fullMode=false
